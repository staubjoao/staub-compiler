importa circulo.jsc;

forma Principal {
    vazio principal() {
        inteiro x;

        Circulo circulo = Circulo();

        circulo.raio = 5.1;

        x = 35;

        inteiro[] x;

        enquanto(inteiro i = 0; i < 10; i++) {
            imprime("teste");
        }

        se(x == 35) {
            imprime("Teste");
        }

        // teste
    }
}